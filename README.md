### Draft Predictor

Making an embedding of all League of Legends champions that I will use to predict what draft is better using a transformer model. The embeddings will be trained on solo q data and then the model using those embeddings will be trained on pro drafts.

The model will predict the chance blue side will win the game as a number. 